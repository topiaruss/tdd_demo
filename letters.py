def vowel_frequency(s):
    """return dict of vowel frequency
       assumes ASCII only.
       TODO: refine spec and requirements re: i18n.
    """
    ret = dict(a=0, e=0, i=0, o=0, u=0)
    return ret